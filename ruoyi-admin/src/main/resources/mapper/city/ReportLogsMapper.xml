<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.city.mapper.ReportLogsMapper">
    
    <resultMap type="ReportLogs" id="ReportLogsResult">
        <result property="id"    column="id"    />
        <result property="reportId"    column="report_id"    />
        <result property="userId"    column="user_id"    />
        <result property="action"    column="action"    />
        <result property="remark"    column="remark"    />
        <result property="createTime"    column="create_time"    />
    </resultMap>

    <sql id="selectReportLogsVo">
        select id, report_id, user_id, action, remark, create_time from report_logs
    </sql>

    <select id="selectReportLogsList" parameterType="ReportLogs" resultMap="ReportLogsResult">
        <include refid="selectReportLogsVo"/>
        <where>  
            <if test="reportId != null "> and report_id = #{reportId}</if>
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="action != null  and action != ''"> and action = #{action}</if>
        </where>
    </select>
    
    <select id="selectReportLogsById" parameterType="Long" resultMap="ReportLogsResult">
        <include refid="selectReportLogsVo"/>
        where id = #{id}
    </select>

    <insert id="insertReportLogs" parameterType="ReportLogs" useGeneratedKeys="true" keyProperty="id">
        insert into report_logs
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="reportId != null">report_id,</if>
            <if test="userId != null">user_id,</if>
            <if test="action != null and action != ''">action,</if>
            <if test="remark != null">remark,</if>
            <if test="createTime != null">create_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="reportId != null">#{reportId},</if>
            <if test="userId != null">#{userId},</if>
            <if test="action != null and action != ''">#{action},</if>
            <if test="remark != null">#{remark},</if>
            <if test="createTime != null">#{createTime},</if>
         </trim>
    </insert>

    <update id="updateReportLogs" parameterType="ReportLogs">
        update report_logs
        <trim prefix="SET" suffixOverrides=",">
            <if test="reportId != null">report_id = #{reportId},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="action != null and action != ''">action = #{action},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteReportLogsById" parameterType="Long">
        delete from report_logs where id = #{id}
    </delete>

    <delete id="deleteReportLogsByIds" parameterType="String">
        delete from report_logs where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>